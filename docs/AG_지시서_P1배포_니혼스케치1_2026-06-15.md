# AG 지시서 — P1 첫 글 배포 (니혼 스케치 #1 / walk)
> 작성: Claude / 2026-06-15
> 검수: Claude 검수 통과 + 승주님 컨펌 완료
> 선결: TASK-0(라우팅 정비) 완료 상태여야 함

---

## 0. 개요

GSFGrace **첫 콘텐츠 배포**다. walk 카테고리, KO/JA 한 쌍.
검수 완료본은 아래 위치에 준비되어 있다 (`draft: false` 이미 설정됨):

```
content-pipeline/_approved/walk_ko_nihon-sketch-1.md
content-pipeline/_approved/walk_ja_nihon-sketch-1.md
```

## 1. 저장 (파일 이동)

`_approved/`의 두 파일을 아래 경로로 복사한다. **파일명은 slug만** 남긴다:

| 원본 (_approved) | → 저장 경로 |
|------|------|
| `walk_ko_nihon-sketch-1.md` | `src/content/walk/ko/nihon-sketch-1.md` |
| `walk_ja_nihon-sketch-1.md` | `src/content/walk/ja/nihon-sketch-1.md` |

- 프론트매터는 그대로 둔다 (이미 `draft: false`).
- KO/JA 동일 slug(`nihon-sketch-1`) — 폴더로 분리되므로 충돌 없음.

## 2. 빌드 검증

```bash
cd scratch/projects/GSF-Grace
npm run build
```
- astro check 통과 확인. 실패 시 **배포 중단·보고**.
- 빌드 후 다음 경로가 생성되는지 확인:
  - JA: `/walk/nihon-sketch-1`
  - KO: `/ko/walk/nihon-sketch-1`

## 3. 라우팅 동작 확인 (TASK-0 검증 겸)

이 글이 **TASK-0에서 만든 walk 상세 라우터의 첫 실사용**이다. 확인:
- [ ] `/walk/nihon-sketch-1` — JA만 렌더링 (KO 글이 섞이지 않음)
- [ ] `/ko/walk/nihon-sketch-1` — KO만 렌더링
- [ ] walk 목록(`/walk`, `/ko/walk`)에 카드가 뜨고 상세로 연결
- [ ] 언어 스위처가 두 글을 상호 연결
- [ ] /story CTA 링크 작동 (JA→/story, KO→/ko/story)
- [ ] hreflang ja/ko 출력

## 4. 배포

- git add (content-pipeline 폴더 이동분 + 신규 콘텐츠 함께)
- commit 예: `content(walk): add nihon-sketch-1 ja/ko (P1 first post)`
- push → Vercel 자동배포.

## 5. 배포 후 보고

```
[배포 완료] walk/nihon-sketch-1 (P1 첫 글)
- JA: https://gsfgrace.com/walk/nihon-sketch-1
- KO: https://gsfgrace.com/ko/walk/nihon-sketch-1
- 빌드: OK
- 라우팅 검증: JA/KO 분리 정상 / 스위처 정상 / CTA 정상
- 특이사항: (TASK-0 라우터 실사용 첫 케이스 — 문제 있으면 명시)
```

> ⚠️ author 필드: 이 글은 승주 작성이라 author 불필요(기본값). author 필드 추가
> 작업(`AG_지시서_author필드추가`)은 마유미 글(P2) 전까지만 끝나면 됨. 이 글과 무관.
