---
/**
 * Footer.astro
 * GSFGrace 푸터 — STEP 2+4: olive-earth 변수화, Manifesto 카피, clay warm CTA
 */
import PathLine from './PathLine.astro';

interface Props {
  lang?: 'ja' | 'ko';
}

const { lang = 'ja' } = Astro.props;

const copy = {
  ja: {
    newsletter: {
      heading: '東京からの手紙',
      body: '新しい記事と、東京での宣教の歩みを、\nときどきお届けします。\n頻度の約束はありません。ただ、誠実に。',
      cta: '手紙を受け取る',
      placeholder: 'メールアドレス',
    },
    prayer: {
      heading: '祈りで共に',
      body: '東京のために祈ってください。\n日本橋に、福音の家族が立てられますように。',
    },
    story: 'はじめての方へ →',
    copyright: `© ${new Date().getFullYear()} GSFGrace — Hodos Tokyo`,
    location: '日本橋, 東京',
  },
  ko: {
    newsletter: {
      heading: '도쿄에서 보내는 편지',
      body: '새 글과 사역 소식을 전해드립니다.\n빈도의 약속은 없습니다. 다만, 성실하게.',
      cta: '편지 받기',
      placeholder: '이메일 주소',
    },
    prayer: {
      heading: '기도로 함께하기',
      body: '도쿄를 위해 기도해주세요.\n니혼바시에 복음의 가족이 세워지도록.',
    },
    story: '처음 오셨나요? →',
    copyright: `© ${new Date().getFullYear()} GSFGrace — Hodos Tokyo`,
    location: '니혼바시, 도쿄',
  },
};

const t = copy[lang];
---

<PathLine />
<footer class="site-footer">
  <div class="footer-inner">
    <!-- 뉴스레터 CTA -->
    <section class="footer-newsletter" aria-labelledby="newsletter-heading">
      <h2 id="newsletter-heading" class="footer-section-title">{t.newsletter.heading}</h2>
      <p class="footer-body">{t.newsletter.body}</p>
      <form class="newsletter-form" action="#" method="POST" aria-label="Newsletter signup">
        <input
          type="email"
          name="email"
          id="footer-email"
          placeholder={t.newsletter.placeholder}
          required
          aria-label={t.newsletter.placeholder}
          class="newsletter-input"
        />
        <!-- clay(#B0875A) — 뉴스레터 버튼 단 1곳 -->
        <button type="submit" class="newsletter-btn">{t.newsletter.cta}</button>
      </form>
    </section>

    <!-- Prayer 섹션 -->
    <section class="footer-prayer" aria-labelledby="prayer-heading">
      <h2 id="prayer-heading" class="footer-section-title">{t.prayer.heading}</h2>
      <p class="footer-body prayer-text">{t.prayer.body}</p>
    </section>

    <!-- Story CTA 링크 -->
    <div class="footer-story-cta">
      <a href="/story" class="story-link" id="footer-story-link">{t.story}</a>
    </div>

    <!-- 하단 바 -->
    <div class="footer-bar">
      <p class="footer-copyright">{t.copyright}</p>
      <p class="footer-location">✦ {t.location}</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: #1c1917;
    color: #d6d3d1;
    padding: 4rem 1.5rem 2rem;
    margin-top: 5rem;
  }

  .footer-inner {
    max-width: 720px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
  }

  .footer-section-title {
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #a8a29e;
    margin-bottom: 0.75rem;
    font-family: var(--font-sans);
  }

  .footer-body {
    font-size: 0.88rem;
    color: #d6d3d1;
    line-height: 1.85;
    white-space: pre-line;
    font-family: var(--font-sans);
  }

  .newsletter-form {
    display: flex;
    gap: 0.5rem;
    margin-top: 0.75rem;
  }

  .newsletter-input {
    flex: 1;
    padding: 0.5rem 0.75rem;
    background: #292524;
    border: 1px solid #44403c;
    border-radius: 4px;
    color: #f5f5f4;
    font-size: 0.875rem;
    outline: none;
    transition: border-color 0.15s;
    font-family: var(--font-sans);
  }

  .newsletter-input:focus {
    border-color: var(--color-warm);
  }

  .newsletter-input::placeholder {
    color: #78716c;
  }

  /* clay — 뉴스레터 버튼 단 1곳 */
  .newsletter-btn {
    padding: 0.5rem 1rem;
    background: var(--color-warm);
    color: #fff;
    border: none;
    border-radius: 4px;
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s;
    white-space: nowrap;
    font-family: var(--font-sans);
  }

  .newsletter-btn:hover {
    background: #9a7349;
  }

  .prayer-text {
    border-left: 2px solid #44403c;
    padding-left: 1rem;
    font-style: italic;
  }

  .footer-story-cta {
    grid-column: 1 / -1;
    text-align: center;
    padding: 1.5rem 0;
    border-top: 1px solid #292524;
    border-bottom: 1px solid #292524;
  }

  .story-link {
    color: #a3b89b;
    font-size: 0.88rem;
    font-weight: 500;
    text-decoration: none;
    transition: color 0.15s;
    /* story-link는 footer 내 accent 역할 — olive와 구분을 위해 유지 */
    color: #a3b89b;
    font-family: var(--font-sans);
  }

  .story-link:hover {
    color: #c5d4bf;
    text-decoration: underline;
  }

  .footer-bar {
    grid-column: 1 / -1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.75rem;
    color: #78716c;
    padding-top: 0.5rem;
    font-family: var(--font-sans);
  }

  .footer-location {
    letter-spacing: 0.05em;
  }

  @media (max-width: 600px) {
    .footer-inner {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .newsletter-form {
      flex-direction: column;
    }

    .footer-bar {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }
  }
</style>
